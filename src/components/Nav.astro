---
const links = [
	{ href: "/#journey", label: "journey" },
	{ href: "/#how-i-work", label: "work" },
	{ href: "/#contact", label: "contact" }
];
---

<header class="bar">
	<a href="/" class="brand mono">◈ N.BURGESS</a>
	<nav class="mono">
		{links.map((link) => <a href={link.href}>{link.label}</a>)}
	</nav>
</header>
